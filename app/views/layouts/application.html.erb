<!DOCTYPE html>
<html>
  <head>
    <title>Tacostagram Final</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!--Installs bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/application.css">
  </head>

  <body>
    <!--Adds Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light" style="background-color: #e3f2fd;">
      
      <div class="container-fluid">
        <a class="navbar-brand" href="/">Home</a>

        <ul class="navbar-nav">
          <% if @current_user %>
              <li class="nav-item d-flex align-items-center">
                <p class="mb-0">Hi <%= @current_user.username %>!</p>
              </li>
              <li class="nav-item">
                <%= button_to "Logout", "/sessions/logout", class: "nav-link btn btn-link", method: :delete %>
              </li>
          <%else%>
              <li class="nav-item">
                <a class="nav-link" href="/sessions/new">Login</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/users/new">Sign Up</a>
              </li>
          <%end%>
        </ul>
      </div>
    </nav>

    <div class="container-fluid px-6 py-3">
      <%= flash[:notice] %>
    </div>

    <!--Wraps yield in container -->
    <div>
      <%= yield %>
    </div>

    <!--Installs bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>

  </body>
</html>
